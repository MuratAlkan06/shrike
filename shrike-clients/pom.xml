<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.shrike</groupId>
    <artifactId>shrike-parent</artifactId>
    <version>1.0.0</version>
  </parent>

  <artifactId>shrike-clients</artifactId>

  <name>Shrike Clients</name>
  <description>Producer and consumer clients, built on the protocol surface of shrike-core.</description>

  <dependencies>
    <dependency>
      <groupId>io.shrike</groupId>
      <artifactId>shrike-core</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- The process harness these tests are built on — a child JVM, a ready-file handshake, and a
           destroy that takes the whole process tree — is the same harness shrike-admin needs to start a
           broker for its own *IT classes. Attaching the test classes as a second artifact publishes it
           once instead of copying it, so there is one place where a bounded wait or a cleanup rule can
           be got wrong. DESIGN.md records the choice. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <executions>
          <execution>
            <id>attach-the-process-harness</id>
            <goals>
              <goal>test-jar</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- The *IT classes start real brokers, producers, and consumers as separate operating-system
           processes, so they run in the integration-test phase against the built artifacts rather
           than beside the unit tests. Surefire ignores an *IT by default; verify is what fails the
           build when one does. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <executions>
          <execution>
            <id>run-integration-tests</id>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <!-- Without this the forked JVM's java.class.path is a manifest-only jar, and the tests
               launch child JVMs with the classpath they were given. -->
          <useManifestOnlyJar>false</useManifestOnlyJar>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
